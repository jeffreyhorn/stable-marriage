# Changelog

## 0.2.0

- Breaking change: the supported root API is now the one-to-one
  `stable_marriage(...)` solver.
- Breaking change: couples support moved behind the experimental
  `stable_marriage.experimental.stable_marriage_with_couples(...)` entry point.
- Breaking change: the minimum supported Python version is now 3.11.
